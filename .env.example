# Used when running backend outside Docker (optional)
DATABASE_URL=postgres://wtf@localhost:5432/wtf_livepulse
PORT=3001
NODE_ENV=development

# Frontend dev (Vite) — optional; Docker build uses nginx proxy
VITE_API_BASE=http://localhost:3001
